# Environment Variables

# Contact form (choose one service)
# Option 1: Resend (recommended)
RESEND_API_KEY=

# Option 2: Leave blank and wire up your own handler in app/api/contact/route.ts

# Site URL (for OpenGraph, canonical URLs)
NEXT_PUBLIC_SITE_URL=https://yourdomain.com
